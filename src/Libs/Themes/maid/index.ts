import BottomNavigationTab from "./components/bottom-navigation-tab";
import Button from "./components/button";
const mapping = {
  components: {
    BottomNavigationTab: BottomNavigationTab,
    Button: Button
  }
};
export const maindThemeMapping = mapping;
export default mapping;
